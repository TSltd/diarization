# ── Build artifacts ──────────────────────────────────────────────────────────
tests/unit_tests
tests/integration_tests
tests/integration_tests_real
tests/acceptance_test
tests/verification_test
tests/verification_trials
bench/diarization_bench
bench/diarization_bench_real
tools/embed_wav
tools/check_trials

# Object files / dependency files
*.o
*.d
*.a
*.so
*.so.*

# ── CI / log files ───────────────────────────────────────────────────────────
*.log
ci_run.log

# ── Core dumps ───────────────────────────────────────────────────────────────
core
core.*
vgcore.*

# ── OS / editor artefacts ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# ── Python virtualenv ────────────────────────────────────────────────────────
.venv/

# ── Python cache ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── CMake build directory ────────────────────────────────────────────────────
build/

# ── Large binary assets (tracked via LFS or separately) ─────────────────────
# testdata/audio/ contains ~10-35 MB WAV files — uncomment to exclude from git
# testdata/audio/

# testdata/verification/ contains 30 generated WAVs from LibriSpeech (~15 MB)
# Regenerate with: python3 tests/gen_verification_wavs.py
testdata/verification/

# wespeaker/ contains the ONNX model (~24 MB) — uncomment to exclude
# wespeaker/

dev/

# testdata/voxceleb_trials/audio/voxceleb1_cd contains 2240 items (2076 .wav files, 164 folders)
# a subset of the voxceleb1 dataset
testdata/voxceleb_trials/audio/voxceleb1_cd/
